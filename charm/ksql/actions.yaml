create-ksql:
  description: Runs the ksql scripts to create streams/tables.
  params:
    entries:
      type: string
      description: YAML formated list of ksql entries to create.
    partitions:
      type: string
      description: number of partitions for ksql topics
  required: [entries]
  additionalProperties: false
